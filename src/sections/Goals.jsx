import { Section } from '../containers';

import './Goals.css';

const Goals = () => (
    <Section id="goals" title="Goals">
      <p>As always, there's an opportunity to solve the issue with creativity and community.</p>
      <p>We have designed and are actively printing a cheap 3D printed version of the drain cover to make it </p>
    </Section>
  );

export default Goals; 